[project]
name = "python-client"
version = "0.9.0"
description = "Python bindings for the Gradient Labs API"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "dataclasses-json>=0.6.7",
    "pytest>=8.3.4",
    "pytz>=2024.2",
    "requests>=2.32.3",
    "ruff>=0.9.1",
]
